import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'strangle-your-friends',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
